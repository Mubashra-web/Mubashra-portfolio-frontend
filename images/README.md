Put your images here:
- profile.jpg — used only if you don't upload a picture via the Admin panel (the admin upload is stored on the backend instead, under /uploads).
- projects/ — optional local images for the Projects section (or just paste an image URL in the admin form).
- icons/ — optional skill icons.
